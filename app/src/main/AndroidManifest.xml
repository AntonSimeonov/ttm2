<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ninja.paranoidandroid.ttm2">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".AddProject" />
        <activity android:name=".InitDb" />
        <activity
            android:name=".AddTask"
            android:label="@string/title_activity_add_task"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ProjectList"
            android:label="@string/title_activity_project_list"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ProjectSpace" />
        <activity
            android:name=".ProjectSpaceTest"
            android:label="@string/title_activity_project_space_test"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ProjectDesk"
            android:label="@string/title_activity_project_desk"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".TaskInfoActivity"></activity>
    </application>

</manifest>